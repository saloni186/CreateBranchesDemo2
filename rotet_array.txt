import java.util.Arrays;
public class RoteteArray{

	public static void main(String[] args){
		
	/*int[] arr={3,5,1,2,6};//6,7,8,3,5,1,2
		 //0,1,2,3,4,5,6  //4,5,6,0,1,2,3

	int[] arr2=new int[arr.length];
	
	for(int i=0;i<arr.length;i++){
	if(i>arr2.length/2){
	//System.out.println(i+"  "+arr2.length/2+"  "+arr2[i]+"   "+arr[i]);
	arr2[i]=arr[arr.length];
	System.out.print("arr2[i] ="+arr2[i]);
	}else{
	arr2[i]=arr[i];
	//System.out.print("arr2[i] ="+arr2[i]);
	//System.out.println(i+"  "+arr2.length/2+"  "+arr2[i]+"   "+arr[i]);

	}
		}*/

int[] arr = {3, 5, 1, 2, 6, 7, 8};
        int k = 3; // number of positions to rotate
        int n = arr.length;

        int[] arr2 = new int[n];

        for (int i = 0; i < n; i++) {
            arr2[(i + k) % n] = arr[i];
	System.out.println((i+k)%n+"= "+arr[i]);
        }

       // System.out.println(Arrays.toString(arr2));
	
//System.out.println(Arrays.toString(arr2));


	/*int[] arr2=new int[arr.length];
	int rotetCount=10%arr2.length;
	
	for(int i=0;i<arr.length;i++){
		if(i<rotetCount){
		arr2[i]=arr[arr.length-rotetCount+i];   
		}else{
		arr2[i]=arr[i-rotetCount];	
		}


	}
System.out.println(Arrays.toString(arr2));*/		


}	
}